#:schema https://developers.openai.com/codex/config-schema.json
# Miftah — Codex CLI Configuration

model = "gpt-5.6-terra"
approval_policy = "on-request"
sandbox_mode = "workspace-write"

[mcp_servers.context7]
command = "npx"
args = ["-y", "@upstash/context7-mcp@latest"]
