{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'input=$(cat); file=$(echo \"$input\" | grep -o '\"file_path\":\"[^\"]*\"' | head -1 | cut -d'\"' -f4); if [[ \"$file\" == *.ts || \"$file\" == *.tsx ]]; then cd ~/miftah && npx tsc --noEmit --pretty 2>&1 | head -20; fi'",
            "timeout": 30
          }
        ],
        "description": "TypeScript check after editing .ts/.tsx files"
      }
    ]
  }
}
