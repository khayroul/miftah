# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env*
!.env.local.example

# vercel
.vercel

# Miftah assets (large binaries — stored in Supabase Storage / CDN)
/assets/pages/*.png
/assets/ayat/*.png
/assets/words/*.png
!/assets/**/.gitkeep

# QCF fonts (604 TTF files per version, ~94MB each — download via script)
/assets/fonts/qcf-v2/*.TTF
/assets/fonts/qcf-v2-woff2/

# Test renders (proof-of-concept output)
/test/qcf_*.png

# Large data files (QUL dump, zips)
data/qul/mini_quran_dev.sql
data/qul/*.zip
data/qul/indonesian-word-by-word-translation.json.zip

# External line-break data (originally from zonetecde/mushaf-layout)
# Committed to repo for Vercel deployment
# data/mushaf-layout/

# Python cache
__pycache__/
*.pyc

# Seed split files (generated, keep only seed.sql)
data/seed/seed_part*.sql

# typescript
*.tsbuildinfo
next-env.d.ts
supabase/.temp/
.tmp/

# PWA generated files (predev/prebuild only; source is scripts/sw.template.js
# plus scripts/render-pwa-artifacts.ts — never edit or commit these outputs)
public/translations/
public/layouts/
public/pwa-config.json
public/sw.js

# Claude Code — track rules/settings, ignore personal data
.claude/*
!.claude/rules/
!.claude/commands/
!.claude/settings.json
